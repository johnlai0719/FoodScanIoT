共用樣式（每個 artboard 自帶一份，畫布上的 artboard 執行時互不共享）

<helmet>
  <style>
    /* 值全部取自 APP/src/screens/HomeScreen.tsx 的 createStyles，未四捨五入 */
    :root {
      --brand: #009B52;        /* BRAND / GREEN — OFF green */
      --brand-light: #E6F7EE;  /* BRAND_LIGHT */
      --cta: #0097A7;          /* CTA — 主要動作，青色不是綠色 */
      --cta-dark: #007380;
      --sel-bg: #E0F5F7;
      --sel-border: #5EC4CC;
      --sel-text: #007380;
      --border: #DDDDDD;
      --ink: #1A1A1A;          /* TEXT_DARK */
      --mid: #757575;          /* TEXT_MID */
      --ground: #F5F5F5;       /* root backgroundColor */
      --hair: rgba(200,184,154,0.4);   /* card borderBottomColor — 暖褐，不是灰 */
      --warn-fg: #B45309;
      --warn-bg: #FEF3C7;
      --warn-line: #FCD34D;
      --bad-bg: #fff1f2;
      --bad-line: #fecaca;
      --bad-title: #7f1d1d;
      --bad-fg: #991b1b;
      /* Nutri-Score 官方色階，取自 components/Gauge.tsx 的 NUTRI_GRADES */
      --g-a: #038141; --g-b: #85bb2f; --g-c: #fecb02; --g-d: #ee8100; --g-e: #e63e11;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0; background: var(--ground); color: var(--ink);
      font-family: system-ui, -apple-system, "Noto Sans TC", "Microsoft JhengHei", sans-serif;
    }
    a { color: var(--cta); } a:hover { color: var(--cta-dark); }
    .scroll { padding: 16px 20px; }                      /* s.scroll */
    .card { padding: 16px 0; border-bottom: 1px solid var(--hair); }  /* s.card */
    .card:last-child { border-bottom: 0; }
    .card-header { display: flex; align-items: center; gap: 8px; margin-bottom: 4px; }
    .card-title { font-size: 14px; font-weight: 800; color: var(--ink); margin: 0; }
    .card-sub { font-size: 11px; color: var(--mid); line-height: 16px; margin: 0 0 4px; }
    .section-label { font-size: 10px; font-weight: 800; color: var(--ink);
                     text-transform: uppercase; letter-spacing: .6px; margin: 0 0 8px; }
    .row { display: flex; align-items: center; gap: 8px; }
    .btn-primary { display: flex; align-items: center; justify-content: center; gap: 6px;
                   padding: 14px; border-radius: 16px; background: var(--cta);
                   color: #fff; font-size: 14px; font-weight: 700; }
    .btn-secondary { display: inline-flex; align-items: center; justify-content: center; gap: 4px;
                     padding: 12px 14px; border-radius: 14px; background: var(--brand-light);
                     border: 1px solid var(--border); color: var(--brand);
                     font-size: 13px; font-weight: 700; }
    .pill { padding: 8px 12px; border-radius: 10px; border: 1px solid var(--border);
            background: var(--brand-light); font-size: 12px; font-weight: 600; color: var(--mid); }
    .pill.on { background: var(--sel-bg); border-color: var(--sel-border); color: var(--sel-text); }
    .input { background: #fff; border: 1px solid var(--border); border-radius: 10px;
             padding: 8px 12px; font-size: 12px; color: var(--ink); }
  </style>
</helmet>

圖示一律 inline SVG（lucide 風格：24 格線、stroke 2、round cap），不用 emoji。
