# Cloud 直連（繞過 Fog）時要送的金鑰，需與 Cloud 的 API_SHARED_SECRET 相同。
# 留空則不送 X-API-Key，Cloud 會回 401——只走 Fog 的話留空沒關係。
#
# ⚠ EXPO_PUBLIC_ 的值會在打包時直接寫進 bundle，拿得到 App 的人就拿得到它。
#   這裡的用意只是不讓金鑰進版控，不是讓它變成真正的祕密。正式路徑是
#   App → Fog → Cloud，只有 Fog 需要持有金鑰（見 server/main.py 的說明）。
EXPO_PUBLIC_CLOUD_API_KEY=
