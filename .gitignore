# 敏感資訊與環境變數
.env
*/.env
*.pem
*.key
secrets.json

# 資料庫實體檔案與大型備份
Cloud_Server/database/mysql_data/
mysql_data/
mysql_data_old/
*.tar.gz
*.zip
*.7z
*.db
*.sqlite
*.ibd
*.dblwr
binlog.*
*.sql
!server/seed_data/*.sql

# 資料與 CSV (通常太大且不適合進 Git)
mysql_csv/
*.csv
*.json
!package.json
!app.json
!tsconfig.json
!metadata.json
!additives_manifest.json
!additives_manifest_pretty.json
!new_risks_data.json

# 程式依賴套件
node_modules/
__pycache__/
.expo/
dist/
build/
.next/
.venv/
env/
venv/

# 專案文件、日誌與個人檔案 (不推送到 GitHub)
*.log
*.pdf
*.jpg
*.png
*.jpeg
*.Identifier
docs/
.gemini/
.learnings/
RoadMap/
*.docx
*.html
*.md
!README.md
# 專案說明書，AI 助理每次開新對話會自動讀取，必須進版控才對交接與隊友有意義
!CLAUDE.md
# 後端串接參考包的說明（記載契約的權威來源與強制點）
!APP/backend-ref/README.md
# npm lockfile。被上方的 *.json 規則擋掉，導致 Pi 上沒有 lockfile、部署時
# npm ci 必定失敗而回退到 npm install（版本可能與開發環境不同）。
# 納入版控後部署才具可重現性。
!fog/package-lock.json
!APP/package-lock.json

# PaddleOCR 本機辨識實驗的報告與原始數據。結論是「不採用」，但過程與數據是
# 「為何改用視覺模型」的依據，論文需要引用，故明確納入版控。
# （原本被上方的 *.md / *.json 規則擋掉，才會一直只存在於 add-app-and-assets 分支。）
!測試/圖片掃描處理/*.md
!測試/圖片掃描處理/*.json

# 量化測試集的文字資產。必須進版控——教授 2026-07-30 回饋要求「用同一組案例」
# 跨期比較 P/R/F1 與延遲，而測試集若不在版控內，「同一組」無從證明，
# 歷次報告的數字也沒有第二個人能重跑驗證。
#   cases.json      案例定義（含 set_version，core48 為凍結子集）
#   ground_truth/   人工正解，唯一不可由程式重生的資產
#   predictions/    已存的辨識結果。離線回測（eval_ingredient_parse.py、
#                   audit_additive_matches.py）吃這批資料，不必重打 API 花錢
#   manifest.json   原圖的 SHA256 清單，見 manifest_tool.py 檔頭
# 原圖本身（167 MB）仍不進版控，由 manifest 保證其身分。
#
# 注意萬用字元只吃一層：ground_truth/ 依類別分了子資料夾
# （ground_truth/<category>/<case_id>.json），故需 ** 而非 *。
# 曾因此漏掉——重整目錄後 48 份正解在 git 眼中變成「全部刪除」，
# 新位置又進不了版控，而 git status 只顯示 D、不會提醒你新檔被忽略。
!測試/量化測試/cases.json
!測試/量化測試/manifest.json
!測試/量化測試/ground_truth/**/*.json
!測試/量化測試/predictions/*.json
!測試/量化測試/README.md
# results/ 每跑一次就覆寫，屬暫存產物，不進版控。
# 但底線開頭的 _baseline_* 是刻意留存的參考點——報告引用的數字、以及日後
# 比較的基準都在裡面，其中延遲與稽核類的數字要重跑得花 API 錢或連本機資料庫，
# 只留在硬碟上等同於沒有備份（測試集本身先前就是這樣差點失去）。
!測試/量化測試/results/_baseline_*/**
!cloud/requirements.txt
!fog/requirements.txt
# tests/ 底下多為需要跑起伺服器與資料庫的臨時整合腳本，預設不進版控。
# 但 tests/contract/ 是跨層契約測試（純函式、CI 會實際執行），必須進版控，
# 否則 CI 上的 pytest 會收集不到任何測試而「假綠」。
# 註：git 無法在父目錄被排除後再重新納入子目錄，故此處用 tests/* 而非 tests/。
tests/*
!tests/contract/
test_img
*.bak
convert_to_pdf.py
update_main.py

# 系統檔案
.DS_Store
Thumbs.db
fog_cache.db
quarto-*.deb
start_app_only.sh
start_backend.sh
資料抓取/

# --- 2026-08-03 首次大量提交前補充 ---
# 暫存與工具產物：內含下載的執行檔（如 typst，54 MB），不應進版控
scratch/
*/scratch/
*.out

# 大型原始資料檔。原始來源可重新下載，見各 README 記載的下載位址
*.xlsx
*.xls

# macOS 在非 HFS 磁碟上產生的中繼資料殘留（._IMG_xxx.HEIC 之類）
._*
.AppleDouble

# 原始照片。回測用的辨識結果存於 predictions/，不需保留原圖
*.HEIC
*.heic
*.webp

# 抓取原文的本機存檔（38 MB）。不進版控：來源網址已隨資料存於資料庫的
# description_sources 欄位，此處僅為抓取當下的快取，需要時可依網址重新取得。
添加物資料庫整理/03_llm_ingestion/source_archive/
