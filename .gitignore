# 敏感資訊與環境變數
.env
*/.env
*.pem
*.key
secrets.json

# 資料庫實體檔案與大型備份
Cloud_Server/database/mysql_data/
mysql_data/
mysql_data_old/
*.tar.gz
*.zip
*.7z
*.db
*.sqlite
*.ibd
*.dblwr
binlog.*
*.sql
!server/seed_data/*.sql

# 資料與 CSV (通常太大且不適合進 Git)
mysql_csv/
*.csv
*.json
!package.json
!app.json
!tsconfig.json
!metadata.json
!additives_manifest.json
!additives_manifest_pretty.json
!new_risks_data.json

# 程式依賴套件
node_modules/
__pycache__/
.expo/
dist/
build/
.next/
.venv/
env/
venv/

# 專案文件、日誌與個人檔案 (不推送到 GitHub)
*.log
*.pdf
*.jpg
*.png
*.jpeg
*.Identifier
docs/
.gemini/
.learnings/
RoadMap/
*.docx
*.html
*.md
!README.md
# 專案說明書，AI 助理每次開新對話會自動讀取，必須進版控才對交接與隊友有意義
!CLAUDE.md
# 後端串接參考包的說明（記載契約的權威來源與強制點）
!APP/backend-ref/README.md
!cloud/requirements.txt
!fog/requirements.txt
# tests/ 底下多為需要跑起伺服器與資料庫的臨時整合腳本，預設不進版控。
# 但 tests/contract/ 是跨層契約測試（純函式、CI 會實際執行），必須進版控，
# 否則 CI 上的 pytest 會收集不到任何測試而「假綠」。
# 註：git 無法在父目錄被排除後再重新納入子目錄，故此處用 tests/* 而非 tests/。
tests/*
!tests/contract/
test_img
*.bak
convert_to_pdf.py
update_main.py

# 系統檔案
.DS_Store
Thumbs.db
fog_cache.db
quarto-*.deb
start_app_only.sh
start_backend.sh
資料抓取/

# --- 2026-08-03 首次大量提交前補充 ---
# 暫存與工具產物：內含下載的執行檔（如 typst，54 MB），不應進版控
scratch/
*/scratch/
*.out

# 大型原始資料檔。原始來源可重新下載，見各 README 記載的下載位址
*.xlsx
*.xls

# macOS 在非 HFS 磁碟上產生的中繼資料殘留（._IMG_xxx.HEIC 之類）
._*
.AppleDouble

# 原始照片。回測用的辨識結果存於 predictions/，不需保留原圖
*.HEIC
*.heic
*.webp

# 抓取原文的本機存檔（38 MB）。不進版控：來源網址已隨資料存於資料庫的
# description_sources 欄位，此處僅為抓取當下的快取，需要時可依網址重新取得。
添加物資料庫整理/03_llm_ingestion/source_archive/
