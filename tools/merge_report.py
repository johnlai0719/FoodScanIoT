#!/usr/bin/env python3
"""產生「融合版」成果報告：把更正直接寫進正文，但仍標明改過什麼。

與 `expand_report.py` 的差別
---------------------------
`expand_report.py` **一字不動原文**，只在旁邊掛註解。那份適合當交接與自我
檢討——看得出「當初打算做什麼、後來為何改」。但它讀起來是對照稿不是報告：
正文說用 AWS，下一段說其實是專用主機，兩個版本同時存在。

這一支把更正**寫進正文**，讀起來口徑統一；代價是原文被蓋掉。為了不讓那條
線斷掉，每一處改動都留一個標記說明原文是什麼、為什麼改。

三種標記，意思不同，不可混用
--------------------------
    【修改】  原文的敘述與事實不符或已過時，正文已改寫。註記裡寫出原文。
    【擴充】  原文沒有的內容，新增。原文未被動到。
    【刪除】  申請書專屬、成果報告不該出現的段落，已移除。註記說明理由。

寫作規則
--------
1. **改動必須是事實問題，不是文風問題。** 覺得句子不漂亮不構成改寫的理由——
   那會讓標記失去意義，讀者無法從標記數量判斷差異有多大。
2. **註記要寫出原文。** 只說「已更新」而不說原本寫什麼，等於把可追溯性丟掉，
   那正是這一版相對於擴充版唯一的損失，不能再放大。
3. **未實作的不可寫成已完成。** 這一版讀起來像成果報告，語氣的權重比擴充版
   重得多——原本靠「⚠ 尚未上線」擋住的過度宣稱，在這裡更容易滑過去。
"""
import argparse
import io
import os
import re
import sys

sys.stdout.reconfigure(encoding="utf-8")

MARK_EDIT = "【修改"
MARK_ADD = "【擴充"
MARK_DEL = "【刪除"


def note(kind, title, lines):
    out = ["", "> **%s｜%s】**" % (kind, title), ">"]
    for l in lines:
        out.append("> " + l if l else ">")
    out.append("")
    return out


# ── 一、正文改寫 ────────────────────────────────────────────────────────────
# (要被取代的原文起頭, 原文結尾, 新正文, 標題, 註記)
# 起頭與結尾都必須在原稿中**唯一**，否則中止。
REPLACEMENTS = [
    ("在技術選型上，Cloud 端採用 Amazon Web", "不須修改核心業務邏輯，確保開發期與正式部署期的一致性。",
     """在技術選型上，Cloud 端採用專用運算主機搭配容器化服務架構，以兼顧高算力推論
需求與資料安全；App 與 Fog 之間、Fog 與 Cloud 之間均採 RESTful API 作為層間
通訊協定，以確保介面規格一致且易於維護擴充。

在硬體部署上，Fog 節點以 Raspberry Pi 5（8 GB RAM）作為實體邊緣運算設備，
部署於靠近使用情境的近端位置，透過 Wi-Fi 與使用者 App 及 Cloud 端通訊。
選用此型號而非更低功耗的機種，是因為霧端需在雲端不可用時獨力執行降階辨識。

Cloud 端部署於專用運算主機（AMD Ryzen 5 5600X、NVIDIA GeForce RTX 4060
8 GB、32 GB RAM，Ubuntu 環境），作為視覺解碼與主資料庫伺服器。服務以容器化
方式建置，具備完整的遷移能力，可於後續直接部署至公有雲而不須修改核心業務
邏輯。""",
     "技術選型與硬體部署",
     ["原文為「Cloud 端採用 Amazon Web Services（AWS）」「Fog 節點以 Raspberry Pi",
      "Zero 2W」「Cloud 端目前以本地伺服器（WSL 環境）作為開發與測試用途」。",
      "",
      "改動理由：模組 A 於執行期改採本地視覺模型推論，需要 GPU，故改用專用主機；",
      "霧端需獨力執行降階辨識，Pi Zero 2W 的記憶體不足以承載。容器化與可遷移性",
      "的敘述維持不變，與原規劃一致。"]),

    ("在加密傳輸層面，所有對外通訊強制採用 HTTPS/TLS 1.2+", "節點且內容於傳輸過程中未遭竄改。",
     """在加密傳輸層面，所有對外通訊強制採用 HTTPS/TLS 加密，防範中間人攻擊（MITM）。
對外公開端點統一由邊緣網路服務反向代理，來源主機不直接暴露於公網。在身分
驗證層面，Fog 與 Cloud 之間的每筆請求均須攜帶共享密鑰標頭，未帶或不符者一律
拒絕。""",
     "通訊安全機制",
     ["原文為「每筆請求均須攜帶 X-Fog-Signature 標頭，以 HMAC-SHA256 演算生成簽章」。",
      "",
      "改動理由：HMAC 簽章**未實作**，執行期採用的是共享密鑰標頭。原文將未實作的",
      "機制寫成已部署，屬於事實錯誤而非取捨差異。",
      "",
      "⚠ 一併記錄一項執行期修補：分析端點對外有三條路徑別名共用同一段處理程序，",
      "而金鑰檢查原本只掛在其中一條上。已於 2026-09-12 補齊，三條均驗證通過。"]),

    ("(1) 使用者於購物現場以 App 掃描商品條碼後，App 將條碼", "先完成請求格式化並查詢本地 SQLite 快取表。",
     """(1) 使用者於購物現場以 App 掃描商品條碼並擷取標示影像後，App 將**條碼與影像**
    送至 Fog。**使用者的健康背景（族群、慢性病、過敏原）不隨請求送出**，
    完整留在裝置本機。Fog 先完成請求格式化並查詢本地 SQLite 快取表。""",
     "個人化條件不再離開裝置",
     ["原文為「App 將條碼 ID 及必要的個人化條件，如過敏原清單或族群屬性，送至 Fog」。",
      "",
      "改動理由：這些是健康資料。原設計要求使用者為了一次購物判讀，把疾病與過敏史",
      "交給部署在賣場的邊緣節點；而個人化所需的運算（關鍵字比對與營養素閾值判斷）",
      "在手機上執行毫無困難，送出去並未換得任何能力。邊緣節點仍保留一道剝除敏感",
      "欄位的處理作為深度防禦，以防舊版 App 仍送出。",
      "",
      "⚠ 連帶的設計後果：後端因此**無法**依使用者條件排序或聚焦提示。後端回傳完整",
      "的判定結果，取捨由 App 端完成。原文於 Fog 模組一節所述之「依使用者條件進行",
      "個人化排序與提示聚焦」亦因此不成立。"]),

    ("模組A負責將異質輸入轉為一致的中介表示。", "作為後續風險推論的標準化輸入。",
     """模組A負責將異質輸入轉為一致的中介表示。**以外包裝標示影像為主要輸入來源**，
經光學辨識取得標示內容後，進行文字清理、單位與格式正規化；產品資料庫僅提供
條碼對應的既有紀錄作為補充。其後，將抽取結果對齊至添加物知識庫的統一識別碼，
並將營養資訊轉換為可計算的指標向量，作為後續風險推論的標準化輸入。""",
     "光學辨識是主要路徑，不是備援",
     ["原文為「資料缺漏時啟用 OCR 補齊標示內容」，將光學辨識定位為資料庫查無時的",
      "補救手段。",
      "",
      "改動理由：執行期的實際順序相反。條碼資料庫的涵蓋率遠低於預期，且既有紀錄的",
      "成分欄位多為簡述，不足以支撐添加物逐項比對。以包裝影像為準另有一項好處：",
      "判定依據與使用者眼前的實物一致，可逐項回指到照片上的位置。"]),

    ("在 LLM 選型上，模組 A 採用 Gemini Flash 2.5", "以減少處理流程中的資訊損失並降低系統複雜度。",
     """在視覺辨識選型上，模組 A 採用本研究實作之雙引擎粗精裁切管線：先以 PP-OCR
對全圖進行粗定位並動態裁切出成分區與營養區，再將兩區交由 HunyuanOCR 進行
局部高保真精讀；非結構化之「商品品名」與「製造廠商」兩欄，另由本地輕量化
語言模型（Qwen3.5-2B）讀取辨識文字後進行語意整理。

此設計切中兩項實務限制：雲端通用多模態模型在密集包裝上容易產生無影像依據的
輸出，而本地視覺語言模型若直接以全圖輸入，則因全域雜訊干擾容易陷入自回歸
重複退化。前置動態裁切同時降低了這兩項風險。""",
     "模組 A 的視覺辨識選型",
     ["原文為「採用 Gemini Flash 2.5 作為核心語言模型…無需獨立 OCR 前處理步驟，",
      "由模型讀取影像後直接輸出結構化 JSON」。",
      "",
      "改動理由與量測依據見後文「模組 A 之辨識品質比較」一節。簡言之，改變的理由",
      "不是通用模型不堪用，而是兩者的失效型態不同——本地管線以漏讀為主，而通用",
      "模型傾向依語言先驗補完。在食安應用中後者是使用者無從分辨的假警示。"]),

    ("跨格式整合引擎，在此架構中，AI on Google", "皆能統一抽取出（1）主要用途、（2）一般安全性、及（3）針對特殊族群之風險建議。",
     """跨格式整合引擎，在此架構中，離線檢索與結構化文獻解析管線不被視為擴充層，
而是資料整合與加工中心，負責將上述擴充層中不同格式（HTML、PDF、純文字）的
資料進行知識歸一化。

異質內容萃取：官方公告與法規文件先行落地，由各來源之網頁結構或掃描文件中
抽取出（1）主要用途、（2）一般安全性、及（3）針對特殊族群之風險建議，
抽取後入庫；查詢時只讀本地知識庫。""",
     "外部知識抽取改為離線檢索",
     ["原文為「AI on Google Search 不被視為擴充層，而是資料整合與加工中心」",
      "「利用 Gemini 模型強大的多模態理解能力…統一抽取」。",
      "",
      "改動理由與模組 A 相同——線上生成式整合無法保證輸出的每一句都能回指到來源",
      "文件；食安提示若無法附上可查證的出處，使用者無從判斷是否採信。",
      "離線管線的代價是資料新鮮度依賴排程更新，此點列為已知限制。"]),

    ("模組C（食安風險引擎）判讀產品與製造商層級的外部證據。", "以維持事件摘要的一致性與可比性。",
     """模組C（食安風險引擎）原規劃判讀產品與製造商層級的外部證據，將產品或製造商
資訊與食安事件資料比對後計算風險等級。**該模組於現行系統中整條停用**，
資料庫中雖已累積 152 筆食安事件紀錄，但查詢路徑以組態旗標關閉，回應內容中的
食安事件摘要欄位亦已自介面契約移除。

停用的理由是資料來源的涵蓋率無法確認。若系統查詢了不完整的事件庫而回報
「查無事件」，使用者會把它讀成「這項產品安全」——但實際上只代表「我們的庫裡
沒有」。在涵蓋率可界定之前，不查詢優於給出可能被誤讀為背書的結果。因此現行
系統的風險判讀集中於產品成分層。""",
     "模組 C 停用",
     ["原文以未來式描述模組 C 的比對與 LLM 摘要流程，讀起來像已上線。",
      "",
      "改動理由：該模組刻意停用，不是未完成的工程。把停用的功能寫成已實作，",
      "在食安報告中是最不該有的一類錯誤。"]),

    ("快取逾期判斷以 TTL（time-to-live）機制實作，預設快取有效期為 24", "確保使用者不會取得過期風險資訊。",
     """快取逾期判斷以 TTL（time-to-live）機制實作，**實作之預設快取有效期為 1 小時**。
縮短的原因是開發與驗證期間需要對同一件商品反覆重新辨識，24 小時的快取會讓
每次改動都看不出效果；此值為單一常數，正式場域部署時可依商品查詢頻率調高。
過期後先嘗試向雲端取得新結果，失敗時回傳已過期的舊快取並附帶警示標記。""",
     "快取存活時間與強制失效",
     ["原文為「預設快取有效期為 24 小時；當食安資料庫發布新公告時，系統可觸發",
      "特定條碼之快取強制失效」。",
      "",
      "改動理由：實作值為 1 小時；強制失效**尚未實作**，因為觸發它的食安事件查詢",
      "路徑目前停用。此項屬補工即可——失效機制本身只是依條碼刪除一列快取，",
      "真正的前提是先決定食安事件資料的涵蓋範圍。"]),

    ("我們的核心技術中，App 模組以 React Native（Expo 54）", "並提供流暢的動畫視覺回饋以強化使用者體驗。",
     """我們的核心技術中，App 模組以 React Native（Expo SDK 57）搭配 TypeScript
開發，嚴格定義介面資料結構以確保與 Fog／Cloud 端通訊型別一致。UI 採用行動端
響應式設計。""",
     "行動端框架版本",
     ["原文為 Expo 54。改動理由：測試裝置上的 Expo Go 僅內建 SDK 57，而 Expo Go",
      "一次只帶一個 SDK 版本，專案必須與之相符。"]),

    ("系統捨棄單一字母分級（A--E），改採成分占比圓餅圖以提供更具透明度的資訊呈現。", "使高血壓或糖尿病使用者能進行個人化判斷，而非僅憑單一等級字母做決策。",
     """系統將單一字母分級（A–E）與成分占比圓環**並列呈現**。外圈以字母等級與其對應
色階呈現整體結論，內部扇區則對應 Nutri-Score V7 各負分項的實際點數貢獻，
以區分各營養素類別；正分項以內圈呈現其抵銷扣分的幅度。

兩者並列的理由是它們回答不同問題：字母回答「這項產品大致如何」，圓環回答
「為什麼」。此設計的核心價值在於精準識別風險來源：同樣為 10 分的兩項產品，
圓環能區分出 A 產品由高糖引起、B 產品由高鹽引起，使高血壓或糖尿病使用者能
進行個人化判斷，而非僅憑單一等級字母做決策。""",
     "字母分級保留，與圓環並列",
     ["原文為「系統捨棄單一字母分級（A–E），改採成分占比圓餅圖」。",
      "",
      "改動理由：實作後改為兩者並列。只有圓環時使用者需自行加總才能得到結論；",
      "只有字母時則無法分辨風險來源——而後者正是原文指出的核心價值，完整保留。",
      "",
      "⚠ 圓環各扇區的比例取自後端回傳的實際點數，包含計 0 分的項目（以其上限值",
      "標示）。早期版本曾以寫死的固定比例繪製，已於執行期改為依實際點數渲染。"]),

    ("僅能在既定知識框架內進行語意整合與生成總結，從根本上杜絕虛假事實的產出。",
     "僅能在既定知識框架內進行語意整合與生成總結，從根本上杜絕虛假事實的產出。",
     """僅能在既定知識框架內進行語意整合與生成總結，**大幅降低**虛假事實的產出。""",
     "「杜絕」改為「大幅降低」",
     ["原文稱可「從根本上杜絕虛假事實的產出」。依實測，這個說法過強。",
      "",
      "即使限定在既有知識框架內生成，本地管線仍量到少量無影像來源之輸出：",
      "品名 3／175 案、製造商 8／175 案（過敏原警語為 0／146）。數量遠低於通用",
      "雲端模型（同欄位分別為 52／171 與 33／147），但不是零。",
      "",
      "本報告因此一律採「大幅降低」等表述，並以「輸出的值在照片上查無此字」",
      "為可重複量測的判準，而非宣稱杜絕。"]),
]

# ── 二、刪除 ────────────────────────────────────────────────────────────────
# (起頭, 結尾, 標題, 理由)
DELETIONS = [
    ("4、 需要教授指導的內容", "**陸、參考文獻**",
     "「需要教授指導的內容」一節",
     ["此節為計畫申請階段填寫的欄位，內容為研究問題界定、分工設計與論文撰寫等",
      "需要指導的項目。成果報告不應保留申請書專屬欄位。",
      "",
      "⚠ 原文此處另有章節編號斷層（由「肆」直接跳至「陸」），一併於此標明：",
      "本報告未設「伍」章，此為原稿既有之編號問題，非本次改動造成。"]),
]

# ── 三、用語替換（時態）────────────────────────────────────────────────────
# 成果報告用完成式。這是**逐字替換**，不逐處標記——那會讓標記淹沒正文；
# 改為在文件開頭統一說明一次。
TENSE = [
    ("本研究擬開發", "本研究開發"),
    ("我們將研究計畫的執行分為二階段", "本研究的執行分為二階段"),
    ("我們將以少量自建商品資料", "本研究以少量自建商品資料"),
    ("我們將實作Cloud模組", "本研究實作Cloud模組"),
    ("我們將實作Fog模組", "本研究實作Fog模組"),
    ("我們以 React", "本研究以 React"),
]

HEADER = """> **【本版本說明】**
>
> 本文件為成果報告之**融合版**：計畫執行期間與原提案不符之處已直接改寫入
> 正文，以維持全篇口徑一致。
>
> 為保留可追溯性，每一處改動於其後以標記區塊說明**原文為何、為何更改**。
> 三種標記意思不同：
>
> - **【修改】** 原敘述與事實不符或已過時，正文已改寫，註記中寫出原文。
> - **【擴充】** 原文沒有的內容，新增；原文未被更動。
> - **【刪除】** 申請書專屬、成果報告不應保留之段落，已移除。
>
> 另有一類未逐處標記者：全篇之未來式敘述（「擬」「將」）已改為完成式，
> 屬體例調整，不涉及事實變動。
>
> ⚠ 另有一份**擴充版**（`成果報告_擴充版.md`）保留原文一字未動、僅以註記
> 對照。需要查證原始提案內容時以該版為準。
"""


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--src", required=True)
    ap.add_argument("--out", required=True)
    a = ap.parse_args()

    text = io.open(a.src, encoding="utf-8").read()
    if MARK_EDIT in text or MARK_ADD in text:
        sys.exit("原稿裡已有標記——請以乾淨的抽出稿為輸入。")

    # 刪除先做：它以「下一個大標」為結尾，改寫過後位置會變。
    for start, end, title, why in DELETIONS:
        i = text.find(start)
        j = text.find(end)
        if i < 0 or j < 0 or j <= i:
            sys.exit("刪除區段定位失敗：%s" % title)
        text = text[:i] + "\n".join(note(MARK_DEL, title, why)) + "\n" + text[j:]

    for start, end, new, title, why in REPLACEMENTS:
        i = text.find(start)
        if i < 0:
            sys.exit("找不到要改寫的原文：%s" % start[:34])
        if text.count(start) != 1:
            sys.exit("改寫起點不唯一：%s" % start[:34])
        j = text.find(end, i)
        if j < 0:
            sys.exit("找不到改寫終點：%s" % end[:34])
        j += len(end)
        text = text[:i] + new + "\n" + "\n".join(note(MARK_EDIT, title, why)) + text[j:]

    # 擴充區塊沿用 expand_report 那一份，不另維護一套——同一份知識放兩個地方
    # 遲早分岔（族群詞彙那次就是這樣）。
    #
    # ⚠ 有些擴充的錨點會落在**已被改寫掉的段落**裡（例如原文的 AWS 段落），
    # 那些的內容已經寫進正文，不需要再掛一次註記；找不到錨點就跳過並列出來，
    # 不中止——中止的話兩支腳本會綁死，改一邊就得同時改另一邊。
    sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
    import expand_report as EX

    # 有兩塊擴充的錨點雖然落在被改寫的段落裡，內容卻**不能丟**——改寫只換掉
    # 了敘述，量測數據與未實作清單並沒有被寫進正文。改錨到新正文上。
    # 其餘被跳過的（硬體、端點代理、離線檢索、模組 C、個人化）內容確實已由
    # 【修改】註記涵蓋，跳過是對的。
    # 融合版裡已由【修改】註記涵蓋、不需要再掛一次【擴充】的。
    # 留著會讓同一件事在相鄰兩段各講一次，讀起來像沒整理過。
    DROP = {"原文需更正：「杜絕」宜改為「大幅降低」",
            "原文需更正：光學辨識不是備援，而是主要路徑"}

    REANCHOR = {
        "現況更新：模組 A 改採雙引擎粗精裁切管線，並實測其優劣":
            ("前置動態裁切同時降低了這兩項風險。", "模組 A 之辨識品質比較"),
        "現況更新：行動端版本與尚未實作的項目":
            ("響應式設計。", "行動端尚未實作之項目"),
    }

    lines = text.split(chr(10))
    inserts, skipped = [], []
    for anchor, before, title, body, srcs in EX.EXPANSIONS:
        if title in DROP:
            continue
        if title in REANCHOR:
            anchor, title = REANCHOR[title]
        idx = [i for i, l in enumerate(lines) if anchor in l]
        if len(idx) != 1:
            skipped.append(title)
            continue
        at = idx[0]
        if not before:
            while at + 1 < len(lines) and lines[at + 1].strip():
                at += 1
            at += 1
        inserts.append((at, EX.block(title, body, srcs)))
    for at, blk in sorted(inserts, key=lambda x: -x[0]):
        lines[at:at] = blk
    text = chr(10).join(lines)

    for old, new in TENSE:
        text = text.replace(old, new)

    io.open(a.out, "w", encoding="utf-8").write(HEADER + "\n" + text)
    print("已寫出 %s" % a.out)
    print("改寫 %d 處、刪除 %d 節、擴充 %d 塊、時態替換 %d 條。"
          % (len(REPLACEMENTS), len(DELETIONS), len(inserts), len(TENSE)))
    if skipped:
        print("跳過的擴充（錨點落在已改寫的段落內，內容已寫進正文）：")
        for t in skipped:
            print("  - %s" % t)


if __name__ == "__main__":
    main()
